This package is the interface to the
version of the [ilisMeta16.ili](../../../../../../tests/data/ilismeta16/IlisMeta16.ili)
shipped with ili2py repo. Whenever ili2py needs updates on that model
workflow is as follows:
1. commit new version of ili
2. create new `imd` from `ili` with ili2c
